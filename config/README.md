# Config Module

Purpose:
- Versioned project configuration and static selection artifacts.

Current files:
- `symbols_v1.json`: locked 150-symbol MVP universe.

Guideline:
- Keep config files versioned and explicit; avoid hidden runtime assumptions.
